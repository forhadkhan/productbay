# Task: Combined Column UX Overhaul

- [x] Update `types/index.ts` to reflect the new `elements` data structure.
- [x] Refactor `ColumnItem.tsx` for new `combined` settings logic.
- [x] Implement Drag & Drop sorting for sub-elements in `ColumnItem.tsx`.
- [x] Add individual settings UI for each sub-element.
- [x] Add "Separator" input for inline layout.
- [x] Update `TableRenderer.php` for backend rendering with backward compatibility.
- [x] Verify functionality and layout in the browser.
